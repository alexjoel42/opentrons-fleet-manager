# Observability_V0 backend API
fastapi>=0.115.0
httpx>=0.27.0
uvicorn[standard]>=0.32.0

# Database (Cloud + Local Agent)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# Auth (Phase 1d)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# passlib 1.7.4 is incompatible with bcrypt 4.1+ (version probe uses removed __about__)
bcrypt>=4.0.1,<4.1.0

# Lint & typecheck (optional but recommended)
ruff>=0.8.0
mypy>=1.13.0
